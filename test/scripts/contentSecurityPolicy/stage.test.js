const {
  connectSrc,
  defaultSrc,
  fontSrc,
  formAction,
  frameSrc,
  imgSrc,
  manifestSrc,
  scriptSrc,
  styleSrc,
  workerSrc,
} = import('../../../acrobat/scripts/contentSecurityPolicy/stage');
